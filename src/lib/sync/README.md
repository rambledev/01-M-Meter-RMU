# src/lib/sync

Sync orchestration between the offline queue and the server — online detection,
auto-sync trigger, retry, error handling.

Not implemented yet — reserved for Phase 5 (Sync).
See `docs/offline-strategy.md` §3–4.
