# src/lib/export

Excel export — Calculation Service (tariff formulas) kept in a separate module
from Excel generation (layout/formatting), so an unfinalized formula never
blocks layout work.

Not implemented yet — reserved for Phase 6 (Excel Export).
See `docs/export-format.md` §3 (Calculation Service).
