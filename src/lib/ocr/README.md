# src/lib/ocr

OCR provider wrapper — kept swappable behind a single interface so the UI and
data model never depend on a specific provider (on-device vs cloud vs hybrid).

Not implemented yet — reserved for Phase 4 (Camera + OCR).
See `docs/ocr-strategy.md` (provider not locked yet).
